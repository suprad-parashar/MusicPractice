/** Default tempo (BPM) for practice playback, songs without a stored tempo, and the metronome. */
export const DEFAULT_PRACTICE_BPM = 120;

/** Default tempo for the Rhythm training tab only (new sessions / unset storage). */
export const RHYTHM_DEFAULT_BPM = 90;
